venv - virtual environment. May not need this.

to run the solution, have python, aws cli, boto3, flask installed 

before running, change the bucketname in the __init__.py file

via command line ( run in admin mode) __init__.py

got to url:

http://127.0.0.1:5000/